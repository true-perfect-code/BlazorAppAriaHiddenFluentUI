@page "/counter"
@* @rendermode InteractiveServer *@

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

@* <button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

<br />
<br />

<h2>Test console error "Blocked aria-hidden on an element...</h2>
<h4>Open browser console and click on button. Error will not be displayed in the console.</h4>
<FluentCard>
    <h2>No error</h2>
    <FluentButton Appearance="@Appearance.Accent" @onclick="GotoCounter">Goto Counter</FluentButton>
    <FluentIcon Value="@(new Icons.Filled.Size48.ArrowCircleLeft())" @onclick="GotoCounter" />
</FluentCard>

<br />
<br />

<h2>With error (when ToolTip activated)</h2>
<FluentIcon Id="idBack" Value="@(new Icons.Filled.Size48.ArrowCircleLeft())" OnClick="@(() => NavigationManager!.NavigateTo("/"))" />

<FluentTooltip Anchor="idBack" Position="TooltipPosition.Bottom">
    "Back"
</FluentTooltip> *@




@code {
    // [Inject]
    // protected NavigationManager? NavigationManager { get; set; }

    private int currentCount = 0;
    // protected bool visible = true;

    private void IncrementCount()
    {
        currentCount++;
    }

    // private void GotoCounter()
    // {
    //     NavigationManager!.NavigateTo("/");
    // }


}
